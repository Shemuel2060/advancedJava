<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>

    <!-- Database connection settings -->
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/react_boot_bmi</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">isaacnewton</property>

        <!-- JDBC connection pool settings (optional) -->
        <!-- Use c3p0 connection pool -->
        <!-- <property name="hibernate.c3p0.min_size">5</property>
        <property name="hibernate.c3p0.max_size">20</property>
        <property name="hibernate.c3p0.timeout">300</property>
        <property name="hibernate.c3p0.max_statements">50</property>
        <property name="hibernate.c3p0.idle_test_period">3000</property> -->

        <!-- JDBC connection pool settings (optional- but use built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- Specify dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to stdout (for debugging) -->
        <property name="hibernate.show_sql">true</property>

        <!-- Drop and re-create the database schema on startup (for development) options: create, update, etc..-->
        <property name="hibernate.hbm2ddl.auto">create</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Mapping files contains all model classes in the package org.shemuel.hibernate.dto-->
        <mapping class="hibernate_maven_spring.Hibernate.demo.dto.UserDetails"/>

    </session-factory>
</hibernate-configuration>
